# requirements.txt — used by Streamlit Cloud
# Keep lightweight: the app only runs inference, not training.
streamlit>=1.35.0
torch>=2.0.0          # CPU-only on Streamlit Cloud is fine for inference
transformers>=4.40.0
sentencepiece>=0.1.99
huggingface-hub>=0.22.0
numpy>=1.24.0
pandas>=2.0.0
accelerate>=0.28.0
gdown>=4.7.0
